{
  "$comment": "When Claude Code is run with this gtd/ folder as the working directory, edits/writes inside the folder are pre-approved, while anything outside the project (the rest of the vault) still requires explicit approval. Combined with CLAUDE.md, this keeps changes confined to gtd/.",
  "permissions": {
    "allow": [
      "Edit",
      "Write",
      "Read"
    ],
    "deny": [
      "Edit(../**)",
      "Write(../**)"
    ]
  }
}
